<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Tema base para Android TV kiosk.
        NoActionBar + fullscreen para eliminar qualquer chrome do sistema.
    -->
    <style name="Theme.TvKiosk" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowIsTranslucent">false</item>
        <!-- Remove animação de entrada/saída de activity -->
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
